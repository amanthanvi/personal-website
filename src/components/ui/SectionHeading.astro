---
interface Props {
  id: string;
  label: string;
}

const { id, label } = Astro.props;
---

<div class="reveal mb-2.5">
  <h2
    id={id}
    class="font-heading text-xl font-semibold tracking-tight text-text md:text-2xl"
  >
    {label}
  </h2>
</div>
